<button
  id="humberger-menu"
  class="humberger-menu"
  aria-label="Open navigation menu"
  aria-expanded="false"
  aria-controls="mobile-drawer"
>
  <span class="humberger-menu__bar"></span>
  <span class="humberger-menu__bar"></span>
  <span class="humberger-menu__bar"></span>
</button>

<style>
  .humberger-menu {
    display: flex;
    flex-direction: column;
    justify-content: center;
    gap: var(--space-sm);
    width: 40px;
    padding: 5px;
    background: none;
    border: none;
    cursor: pointer;
    -webkit-tap-highlight-color: transparent;
  }

  .humberger-menu__bar {
    display: block;
    width: 100%;
    height: 2.5px;
    background: var(--secondary-600);
    border-radius: 1px;
    transition:
      transform 0.3s var(--ease-out-expo),
      opacity 0.2s;
  }

  @media (width >= 768px) {
    .humberger-menu {
      display: none;
    }
  }
</style>
